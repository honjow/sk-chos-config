#!/bin/bash

if [ "$EUID" -ne 0 ]; then
    echo "This script must be run as root or with sudo."
    exit 1
fi

src=/home/.nix
dst=/nix

frzr-unlock

# mkdir -p $src
# mkdir -p $dst

# # bind mount
# echo "Bind mounting $src to $dst..."
# mount --bind $src $dst